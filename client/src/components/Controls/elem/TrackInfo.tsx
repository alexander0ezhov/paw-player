import React from "react";
import { IComponentProps } from "@root/global-types";

const TrackInfo: React.FC<IComponentProps> = ({ className }) => {
  return <div className={className}></div>;
};

export default TrackInfo;
