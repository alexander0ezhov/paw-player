import React from "react";
import { IComponentProps } from "@root/global-types";

const Actions: React.FC<IComponentProps> = ({ className }) => {
  return <div className={className}></div>;
};

export default Actions;
