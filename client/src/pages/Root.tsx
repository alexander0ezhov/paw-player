import React from "react";
import { IComponentProps } from "@root/global-types";
import PageWrapper from "@components/common/PageWrapper";

const Root: React.FC<IComponentProps> = () => {
  return <PageWrapper></PageWrapper>;
};

export default Root;
